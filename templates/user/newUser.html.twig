{# templates/user/newUser.html.twig #}
{% extends 'films/baseFilms.html.twig' %}

{% block contenido %}
    <h1>Crear Nuevo Usuario</h1>
    {{ form_start(userForm) }}
        {{ form_widget(userForm) }}
        <button class="btn">Crear Usuario</button>
    {{ form_end(userForm) }}
{% endblock %}
