<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css" integrity="sha384-JcKb8q3iqJ61gNV9KGb8thSsNjpSL0n8PARn9HuZOnIxN0hoP+VmmDGMN5t9UJ0Z" crossorigin="anonymous">
        <title>{% block title %}Welcome!{% endblock %}</title>
        {% block stylesheets %}{% endblock %}
    </head>
    <body>
         <div class="container">
            <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
                <a class="navbar-brand" href="#"><img height="35px" src="https://img.freepik.com/vector-gratis/logotipo-produccion-cine-creativo_23-2149509983.jpg"></a>
                <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavAltMarkup" aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
                    <span class="navbar-toggler-icon"></span>
                </button>
                <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
                    <div class="navbar-nav">
                        <a class="nav-link" href="{{ path('filmsList') }}">Ver películas</a>
                        <a class="nav-link" href="{{ path('uploadFilm') }}">Publicar una película</a>
                        {% if app.user %}
                            <a class="nav-link" href="{{ path('logOut') }}">Cerrar sesión</a>
                        {% else %}
                            <a class="nav-link" href="{{ path('logIn') }}">Iniciar sesión</a>
                            <a class="nav-link" href="{{ path('newUser') }}">Crear cuenta</a>
                        {% endif %}
                    </div>
                </div>
            </nav>
           
            {% block contenido %}
            {% endblock %}
        </div>  
    </body>
</html>
